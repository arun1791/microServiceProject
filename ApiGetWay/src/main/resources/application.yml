server:
  port: 8084

spring:
  application:
    name: API-GETWAY
  cloud:
    gateway:
      routes:
        - id: USER-SERVICE
          uri: lb://USER-SERVICE
          predicates:
            - Path=/users/**

        - id: HOTEL-SERVICE
          uri: lb://HOTEL-SERVICE
          predicates:
            - Path=/hotels/**,/staff/**

        - id: RATING-SERVICE
          uri: lb://RATING-SERVICE
          predicates:
            - Path=/ratings/**

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka


#ockta comfiugutaytion
okta:
  oauth2:
    issuer: https://dev-45141605.okta.com/oauth2/default
    audience: api://default
    client-id: 0oa8zzcbj2pa1E2Kq5d7
    client-secret: Mcw8AdfFnXSZEGFSb9yR3jkkY3CnKYBe40jNfXqf
    scopes: openid, profile, email, offline_access

